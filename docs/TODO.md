# TODO.md - Implementation Roadmap

---

## TODO NEXT

- [ ] Optional: store full request body for raw JSON view (currently prompt_text + response_text).
- [ ] Optional: Status filter in Request History table should be a dropdown with options: All, ...
- [ ] Optional: Limit filter in Request History table should be default to 100 and remember the last used value within the browser session. 

---
