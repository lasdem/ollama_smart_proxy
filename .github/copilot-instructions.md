# Copilot / Agent Instructions

Follow the instruction laid out in the `.cursor/rules` directory. The rules are written in Markdown format and specify how the Copilot or Agent should behave when generating code or providing suggestions.